쓰레드
sleep()
현제 쓰레드를 지정된 시간 동안  멈추게 한다. static 쓰레드 자기자신에 대해 사용하는 메소드

static void sleep(long millis);
-> 꺠우는법 time up interupted



스트림

Stream<T> = Collection.stream();

스트림만들기 -> 중간연산 0~n번 (스트림) -> 최종연산1번(스트림의 요소를소모 1번) => 결과

